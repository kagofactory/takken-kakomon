# CLAUDE.md

宅地建物取引士（宅建・宅建士）試験の過去問演習サイト。静的HTML/CSS/JS、フレームワーク不使用。
`C:\Users\allin\sharoushi-kakomon`（社労士過去問ラボ）の構成をベースに複製・改修して2026年9月13日に新設。

## 経緯・方針決定の記録

- ユーザーが「社労士過去問.comのノウハウを使って次の資格サイトを作りたい。論文形態は難しいので
  それ以外」という相談から開始。宅建・行政書士等の択一式資格を比較検討し、**受験生のパイが大きい**
  ことを理由に宅建を選択（行政書士は記述式が一部あり自動採点に不向きという判断材料もあった）。
- **【2026年9月13日追加指示】品質の年次方針**：ユーザーより「古い年度の簡素化はあとの課題とし、
  まず直近15年分（令和7年度〜平成23年度、2025〜2011）は社労士過去問.comと同水準の解説・条文根拠を
  付ける。その15年分が終わったら、それより古い年度（昭和63年度〜平成22年度、2010年以前、24年分）の
  解説方法についてユーザーに相談する」との指示があった。したがって**当面の実データ収集フェーズ1の
  最終目標は「直近15年分（2025〜2011年度）を社労士版と同水準で完成させること」**であり、それより
  古い年度には着手しない（着手前に必ずユーザーに確認する）。
- 収録年数の方針（最終目標、上記フェーズ1完了後に着手）：最大手の競合サイト「宅建試験過去問道場」（takken-siken.com）は平成12年(2000)〜
  令和7年(2025)の26年分・1,400問を収録している。当初「RETIO公式PDFは平成16年(2004)年分まで」と
  誤って想定していたが、RETIO公式サイト（https://www.retio.or.jp/exam/past_ques_ans/other/ ）を
  直接確認したところ、実際は**昭和63年度(1988、制度開始年）〜令和7年度(2025)の全38年分**が
  公式PDFで公開されていることが判明した（2026年9月13日訂正）。この訂正を踏まえ、ユーザーの判断で
  **公式PDFがある全38年分（S63〜R7）を最終収録目標**とすることに決定した。これは競合の26年分を
  大きく上回る。ただし解説執筆量は当初想定（22年分）の約1.7倍になるため、社労士版（10年分）より
  大幅に長期のプロジェクトになる見込み。

## 宅建試験の構成（社労士との違い）

社労士は7科目×肢別（各科目50肢）だったが、宅建は**1年50問を4分野に配分**する構成：

- 権利関係（民法・借地借家法・区分所有法・不動産登記法等）：例年14問
- 宅建業法：例年20問
- 法令上の制限（都市計画法・建築基準法・国土利用計画法等）：例年8問
- 税・その他（税法・地価公示法・需給関係・土地建物の統計等）：例年8問
- 登録講習修了者は上記のうち5問（税・その他の一部）が免除され45問

`scripts/build_pages.py`の`SUBJECT_META`で `s0`=権利関係, `s1`=宅建業法, `s2`=法令上の制限,
`s3`=税・その他 と定義した（社労士のs0〜s6の7分割から4分割に変更）。

宅建の本試験は元々「4肢のうち1つだけを選ぶ単一正解形式」であり、社労士のように5肢から
「正しいものはどれか」を推測で復元する必要がない（本試験の出題文自体が単一正解を前提にしている）。
ただし、データは社労士と同じ**肢別（1肢＝1レコード、○/×判定）形式**で持つ設計を踏襲した。
理由：(1) 肢別1問1答での学習が可能になる、(2) `js/app.js`の`deriveQuestionSpec()`
（正誤分布から単一正解を自動導出する既存ロジック）がそのまま流用でき、4肢実戦モードも
追加データなしで実現できるため。`number`フィールドは「問1-A」のように付番し、
`js/app.js`の`compareByNumber`/`numberSortKey`の正規表現（`[A-E]`）はA〜Dの4文字にも
そのままマッチするため変更不要。

## 構成（社労士版との差分）

- `index.html` / `css/style.css`（無変更で流用） / `js/app.js` — 演習アプリ本体。
  `PROGRESS_KEY`を`takken_progress_v1`に変更（localStorageキーの衝突回避）。
  「5択実戦」→「4択実戦（A〜Dをまとめて解答）」に表記変更。それ以外のロジック
  （肢別/グループモード切替、続きから始める機能、進捗バー等）は無変更で流用。
- `scripts/build_pages.py` — `SITE_URL`を`https://takken-kakomon.com`に、`SUBJECT_META`を
  上記4分野に変更。サイト名は「宅建過去問ラボ」。社労士版にあった`topics.html`/`topics2.html`
  （法改正・白書対策ページ）は宅建では未設置のため、sitemap生成対象・ヘッダーナビからも除外した。
  必要になれば別途追加する。
- `data/*.json` — 問題データ。**現時点では`sample.json`のデモ2問のみ**。実データは未収録。
- `terms.html`（第5条・著作権）— ユーザーより「著作権の確認が済んでいる資格しか候補に
  挙げていない」旨の指示があり（2026年9月13日）、宅建についても著作権面の問題はないという
  前提で進めることになった。これを受けて社労士版と同じ断定的な表現（「文言を改変せず使用」）に
  更新済み。**ただし社労士のケースと異なり、運営者がRETIOに個別に電話等で確認した記録は本
  プロジェクト内には存在しない**。この前提の根拠を尋ねられた場合や、外部から著作権について
  指摘を受けた場合は、ユーザーに確認を取ること。

## 進め方の合意（2026年9月13日、ユーザー指示）

「3年分すむたびに聞いてください。それ以外はそのまま（自律的に）進めてください」との指示。
したがって、**3年度分のデータ収集が完了するたびにユーザーに進捗を報告し、方針確認を仰ぐ**。
それ以外の細かい区切り（1科目ごと等）では都度質問せず、淡々と収集を継続する。

**進捗（2026年9月13日）**：令和7年度（2025）・令和6年度（2024）の2年分が完成（合計319
レコード）。両年度とも権利関係14問・宅建業法20問・法令上の制限8問・税その他8問の計50問で、
全問RETIO公式PDFの正解番号表と一致確認済み。令和7年度はPlaywrightで単問モード・4択実戦
モードの両方を全問自動操作しコンソールエラー0件を確認済み。

**ユーザー指示により、ここで一旦作業を停止中（2026年9月13日、AIの利用時間上限のため）。
次回セッションでは令和5年度（2023）から再開すること。** 令和5年度の作業手順は令和7・6年度と
全く同じ（RETIO公式PDF `R5_qestion_answer　.pdf`〔原文ママ、全角スペース・誤字あり〕を
ダウンロード→PyMuPDFで画像化→問題文を視覚的に読み取り→末尾ページの正解番号表と突き合わせ→
科目ごとに`rYYYY-sN.json`を作成→`exams.json`に登録→`build_pages.py`実行→問番号1-50の
抜けがないか機械的に確認→コミット）。3年度分（令和7・6・5）が完成した時点でユーザーに報告する
という約束がある。

## 品質管理上の教訓（2026年9月13日、令和6年度収録中に発覚）

令和6年度のデータ収集中に2種類のミスが発覚し、修正した。今後の収録作業では以下を徹底すること。

1. **正解番号表の転記ミス**：分析の途中で正解番号を記憶に頼って参照し、実際にPDFから読み取った
   表とズレた値を使ってしまうことが複数回発生した（令和7年度・令和6年度とも発生）。
   **対策**：年度の作業を始める際に正解番号表の画像を1回読み取り、その場でテキストファイル等に
   書き出して、以降はその書き出した値だけを参照する（記憶で答え合わせをしない）。析出した問題
   ごとの答えが「あれ、思っていたのと違う」となったら、まず記憶ではなくその書き出したテキストを
   再確認する。
2. **ページ送りの見落としによる問題の欠落**：PDFページを2ページずつ読み進める際、1ページに
   複数問（例：問13・14）が収まり、次のページの冒頭に新しい問題（問15）があるという
   構成の境目で、問15をまるごと見落として次のブロック（問16・17）に進んでしまうミスが発生した。
   **対策**：年度・分野が完成した時点で、`python -c "... nums = sorted(set(...))"`のような
   ワンライナーで収録済みの問番号一覧を出し、**連番に抜けがないか必ず確認する**
   （本CLAUDE.mdの「データを追加・更新したら」の節にあるビルドコマンドとは別に、この確認も
   毎回行うこと）。

## 判明しているPDFリンクの規則（2026年9月13日確認）

RETIO公式サイトのPDFファイル名規則（`https://www.retio.or.jp/wp-content/uploads/...`）：
- 平成期（S63〜H30）：`{年度コード}-q_a.pdf`（例：`2024/10/H01-q_a.pdf`〜`H30-q_a.pdf`、
  `2024/10/S63-q_a.pdf`）。問題・正解が1ファイルに統合。
- 令和1〜4年度：`R1-q_a.pdf`（2024/10）、`R2-question.pdf`+`R2-question_002.pdf`（2024/10、
  分割）、`R3-question.pdf`+`R3-question_002.pdf`（2024/12、分割）、`R4-q_a.pdf`（2024/10）
- 令和5〜7年度：`R5_qestion_answer　.pdf`（原文ママ、ファイル名に全角スペース含む・2025/03、
  つづりも"qestion"と誤字）、`R6_question_answer.pdf`（2025/03）、`R7_question_answer.pdf`
  （2025/12）
- 全リンクは一覧ページ https://www.retio.or.jp/exam/past_ques_ans/other/ から取得可能
  （直接HTMLをcurl等で取得してhref="*.pdf"を抽出するのが確実。WebFetchの要約だけに頼ると
  年度・件数を誤ることがあるため注意——実際に本プロジェクトでも「2004年までしかない」という
  誤った要約が最初に出た）

## 法改正への注意（ユーザー指示、2026年9月13日）

「法律が変わって答えが変わる問題は注意して」との指示を受けた。全38年分（昭和63年度〜令和7年度）
という長期間を扱うため、社労士版（10年分）以上に法改正をまたぐ設問が多く出る見込み。
データスキーマの`law_status`（valid/amended/repealed/unverified）・`law_status_note`・
`law_reference_date`フィールド（社労士版から流用済み）で必ず対応すること。

収録時に特に注意すべき既知の大きな制度変更（この他にも年度ごとに個別確認が必要）：

- **民法（債権法）大改正**：2020年4月1日施行。消滅時効・法定利率・保証・瑕疵担保責任（契約不適合
  責任への変更）等、権利関係分野の多数の設問に影響。2019年度以前の問題は改正前の民法が出題対象。
- **民法（成年年齢引下げ）**：2022年4月1日施行、成年年齢が20歳→18歳に。制限行為能力者に関する
  設問に影響。
- **「宅地建物取引主任者」→「宅地建物取引士」への名称変更**：2015年4月1日施行（宅建業法改正）。
  2014年度以前の問題では試験自体の名称・独占業務の位置づけが現行と異なる。
  出典表記時に原文ママか現行名称への読み替えかを確認すること。
  （社労士版でも「被保険者証」→「資格確認書」等、取得元サイトが用語を現代化しているケースが
  複数見つかった実例があるため、同様の注意が必要）
- **相続法改正**：2019年7月1日施行（配偶者居住権の新設等）。権利関係の相続分野に影響。
  区分所有法改正（2002年、2016年等）、宅建業法の重要事項説明のIT化・オンライン化（2017年〜）等も
  年度によって影響する可能性がある。
- 古い年度（特に平成一桁台・昭和63年度）は、上記以外にも現行法と大きく異なる可能性が高いため、
  収録前に該当年度の法令基準日時点の条文を個別に確認すること。

## データ収集で判明した重要な設計上の注意点（2026年9月13日、令和7年度問1〜6着手時）

- **PDFの文字抽出について**：RETIO公式PDFは特殊フォント（CID系）が使われており、`pypdf`等の
  標準テキスト抽出では文字化けする（社労士版の統計PDFで見つかった問題と同種）。本プロジェクトでは
  **PyMuPDF(fitz)でページを画像化し、Claude自身が画像を視覚的に読み取って書き起こす**方式を採用した
  （`get_pixmap(dpi=200)`で十分な解像度が得られることを確認済み）。WebFetchでの直接抽出は、
  試験問題の逐語的な複製に対する慎重な姿勢から拒否されることがあるため使わない。
- **正解番号表**：PDFの末尾ページ（問題本編の後）に「◆試験問題の正解番号表」として掲載されている。
  令和7年度は30ページ中の最終ページ（p30）に掲載。
- **「個数問題」「組み合わせ問題」は肢別に分解しない（重要な設計判断）**：宅建の本試験には
  以下の3パターンがある。
  1. 通常形式：肢1〜4がそれぞれ独立した完全な記述で、「正しいものはどれか／誤っているものは
     どれか」を問う → 社労士と同じく**肢別（A〜D）に分解**し、`js/app.js`の`deriveQuestionSpec()`
     に4択実戦モードの判定を任せてよい（4肢中1肢だけ正誤が異なる構造になるため）。
  2. 個数問題：ア〜エの4つの記述の正誤を判定させ、「誤っているものはいくつあるか」等を選択肢
     1〜4（一つ/二つ/三つ/四つ）で答えさせる形式。
  3. 組み合わせ問題：ア〜エのうち条件に該当するものの組み合わせを選択肢1〜4（例：「ア、エ」
     「イ、ウ」等）から選ばせる形式。

  **2と3は、ア〜エを肢別（A〜D）に分解して保存してはいけない**。理由：`deriveQuestionSpec()`は
  正誤の分布（correctItems.length===1かwrongItems.length===1か）だけを見て「正しいものはどれか
  ／誤っているものはどれか」形式と判定するため、個数問題でたまたま「4つ中1つだけ正しい（または
  誤り）」という分布になった場合、本来「いくつあるか」を問う問題であるにもかかわらず「どれが
  正しいか選べ」という誤った形式で出題されてしまう（令和7年度問3で実際にこの分布が発生することを
  確認した：ア〜エのうち正しいのはエの1つだけで、機械的に分解すると誤判定される）。組み合わせ
  問題も同様に、選択肢のテキスト自体（「ア、エ」等）が実際の出題形式そのものであり、正誤分布からの
  自動導出では再現できない。

  → **個数問題・組み合わせ問題は、社労士版以前の`sample.json`にあった「1問1レコード、
  choicesは選択肢1〜4の完全な文章、answerは正解の選択肢番号」という、分解しない形式で保存する**。
  この形式のレコードは`number`にA〜Eのようなアルファベット接尾辞を付けないため（例：
  `"number": "問3"`）、`js/app.js`の`buildGroups()`（`/^(問\d+)-([A-E])$/`で判定）に
  グループ化対象として拾われず、自動的に肢別（単問）モードでのみ出題される。4択実戦モードでは
  出題されないが、実際の出題形式を誤って再現するよりも安全である。
- **法令基準日**：宅建試験は「その年の4月1日時点で施行されている法令」に基づいて出題される
  （大手予備校等で広く知られている慣行）。令和7年度なら令和7年4月1日。年度が変わるたびに
  確認すること（社労士と同様、年度によって基準日がずれる可能性もあるため過信しない）。
- **科目区分（問番号との対応）**：一般的な宅建試験の構成は 問1〜14＝権利関係、問15〜22＝
  法令上の制限、問23〜25＝税・その他、問26〜45＝宅建業法、問46〜50＝税・その他（5問免除科目：
  住宅金融支援機構法・景品表示法・統計・土地・建物）という並びが定着している。ただし年度に
  よって多少の変動がありうるため、実際の問題文の内容（テーマ）で都度確認すること（機械的に
  問番号だけで判定しない）。

## 未着手・今後のタスク（重要）

0. **【最優先・現在進行中】直近15年分（令和7年度〜平成23年度、2025〜2011）を社労士版と同水準で
   完成させる**。1年度分が完了するたびにexams.jsonのラベルから「作業中」の注記を外すこと。
   **15年分が完了したら、それより古い年度（2010年度以前、24年分）の解説方法についてユーザーに
   相談してから着手する**（簡素化するか、同水準を維持するか等、ユーザーの判断を仰ぐ）。
1. **データ収集（フェーズ1：直近3年度、令和5〜7年度から着手）**：
   - 取得元は上記PDFリンク（令和5〜7年度分）
   - 解説は実施機関が公表しないため、社労士版と同様に**全問ゼロから自前で執筆**
     （宅建業法・民法・都市計画法等の条文根拠を`related_articles`に明記）
   - 社労士版の`scripts/fetch_one.py`（WebFetch+Read PDF技法、文字化けに強い）を参考に
     PDF抽出スクリプトを作成すること
2. **フェーズ3：残り年度への拡張（令和4年度〜昭和63年度、約35年分）**：フェーズ1と同じ方式で
   1年度ずつ順次拡張（社労士版と同じ進め方）。古い年度は制度が現行法と大きく異なる場合がある点に
   注意（`law_status`フィールドで対応）。
3. **ドメイン取得・公開**：`takken-kakomon.com`は2026年9月13日時点でDNS未設定（未使用の可能性が
   高いが、正式な空き状況は未確認）。ユーザーがお名前.com等で取得後、GitHub Pages・CNAME・
   DNS設定を社労士版と同じ手順で行う。
4. **画像**：`images/og-image.png`は社労士版のものをまだコピーしていない（自サイト用に別途用意
   する必要がある）。
5. **GitHubリポジトリ**：まだ作成していない。リポジトリ名・公開/非公開をユーザーに確認のうえ作成する。

## データを追加・更新したら

`data/*.json` を変更したら、必ず以下を実行して `q/` 以下と sitemap.xml を再生成すること。

```bash
cd scripts && python build_pages.py
```

## 運営・訂正フロー・品質チェックリスト

社労士版（`C:\Users\allin\sharoushi-kakomon\CLAUDE.md`）の「解説執筆の品質チェックリスト」
「運営・訂正フロー」「対外的なトラブルへの方針」の各節は、資格が宅建士に変わる以外はそのまま
準用する（法令基準日の明記、実施団体公表資料への紐づけ、`review_status`のデフォルト
`ai_unreviewed`、通説・少数説の両論併記、等）。
